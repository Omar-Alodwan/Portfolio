// Configuration template file for API keys
// This file is safe to commit to version control

// For local development:
// 1. Copy this file to config.js
// 2. Add your actual API key
// 3. config.js is gitignored and won't be committed

// For GitHub Pages deployment:
// The API key will be entered through the UI when needed

window.config = {
    GEMINI_API_KEY: '' // Leave empty - will prompt user to enter key in UI
};

